/**Компонент, отображающий курс валют,
 * является импортируемым компонентом в компонент Header**/

export default function ExchangeRate (props) {
    return (
        <p className="placeholder-glow ExchangeRate">
            <span className="placeholder col-12">ExchangeRate component</span>
        </p>
    );
}
